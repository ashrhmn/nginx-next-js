import React from "react";

const AboutWithId = (props: any) => {
  return (
    <div>
      Project AboutWithId <pre>{JSON.stringify(props, null, 2)}</pre>
    </div>
  );
};

export default AboutWithId;
